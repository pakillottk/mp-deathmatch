# Variables de entorno del CLIENTE (Vite)
# En desarrollo: no hace falta nada, conecta a localhost:2567 por defecto
# En produccion: poner la URL del servidor Colyseus en Render o VPS

# VITE_SERVER_URL=wss://tu-servidor.onrender.com
# VITE_SERVER_URL=wss://tudominio.com
